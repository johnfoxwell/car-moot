<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{description}}</p>
  <p>Created by {{user.name}}</p>
  <p>
    <span for="img" aria-label="car">🚗</span>
    <span class="transmission">{{transmission}}</span>
    transmission rocks!
    <span class="drivetrain">{{drivetrain}}</span>
  </p>
  <p>
    <button class="btn btn-primary">Checkout this Car!</button>
  </p>
</div>
